{
	"type": "object",
	"properties": {
		"header.fixedHeader": {
            "group": "layout",
            "subtab": "header"
        ,	"type": "boolean"
		,	"title": "Fixed Header"
        ,	"description": "Fix header to the top of the page."
        ,   "default": false
		}
	},
    "modifications": [
      {
        "target": "$.properties['header.logoUrl'].default",
        "action": "replace",
        "value": "img/Summit_Logo.png"
      }
    ]
}
